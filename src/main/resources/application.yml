server:
  port: 8081

spring:
  application:
    name: admin-web
  datasource:
    url: jdbc:mysql://localhost:3307/billing_system
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

redis:
  stream:
    message:
      key: message-stream